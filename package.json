{
  "name": "puppeteer-server",
  "version": "1.0.0",
  "description": "Scraping Server",
  "main": "index.js",
  "scripts": {
    "start": "echo '--- Running in EXECUTION environment ---' && ls -laR && echo '--- Now, attempting to start Node server ---' && node index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
